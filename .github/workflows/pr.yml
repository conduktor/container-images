name: pr

# On every PR that touches an image config or the build/CI plumbing, build each
# affected image locally with apko (no push, no signing) and scan it with Trivy +
# Grype. Scanning is report-only: the counts land in the job summary and in one
# sticky PR comment. See AGENTS.md rule 11 for why there is no CVE gate yet.
on:
  pull_request:
    paths:
      - "images/**"
      - "build.sh"
      - "scripts/**"
      - "Makefile"
      # Whole tree, not just workflows/: the CI-only scripts and the composite
      # actions live here too, and they must not be able to change untested.
      - ".github/**"
  workflow_dispatch: {}

permissions:
  contents: read
  pull-requests: write # the `report` job upserts the sticky CVE comment

concurrency:
  group: pr-${{ github.ref }}
  cancel-in-progress: true

env:
  APKO_VERSION: v1.2.30

jobs:
  # Unconditional: these scripts feed both workflows, and a broken images.json
  # would otherwise only surface mid-nightly.
  scripts:
    name: shellcheck + script tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: shellcheck
        run: make lint-shell

      - name: Script unit tests
        run: make test

  detect-changes:
    name: detect changed images
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.build.outputs.matrix }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Compute changed image matrix
        id: build
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          # A change to build.sh, scripts/, workflows or the inventory itself is
          # cross-cutting and rebuilds everything.
          set -euo pipefail
          all="$(jq -c '[.[].dir]' images/images.json)"
          if [ -z "${BASE_SHA:-}" ] || [ -z "${HEAD_SHA:-}" ]; then
            echo "matrix=${all}" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          changed_files=$(git diff --name-only "${BASE_SHA}" "${HEAD_SHA}")
          if echo "${changed_files}" \
            | grep -Eq '^(build\.sh|scripts/|\.github/|images/images\.json$)'; then
            echo "matrix=${all}" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          set +e
          # `[^/]+/` so top-level files under images/ aren't read as image dirs.
          dirs=$(echo "${changed_files}" | awk -F/ '/^images\/[^/]+\// {print $2}' | sort -u)
          set -e
          if [ -z "${dirs}" ]; then
            echo "matrix=[]" >> "$GITHUB_OUTPUT"
          else
            json=$(printf '%s\n' "${dirs}" | jq -R . | jq -s -c .)
            echo "matrix=${json}" >> "$GITHUB_OUTPUT"
          fi

  build-and-scan:
    name: build+scan ${{ matrix.image }}
    needs: detect-changes
    if: needs.detect-changes.outputs.matrix != '[]'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        image: ${{ fromJson(needs.detect-changes.outputs.matrix) }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install apko
        uses: ./.github/actions/setup-apko
        with:
          version: ${{ env.APKO_VERSION }}

      - name: Collect melange configs
        id: melange
        env:
          DIR: ${{ matrix.image }}
        run: |
          set -euo pipefail
          configs="$(cd "images/${DIR}" \
            && find . -maxdepth 1 -name 'melange*.yaml' -printf '%f\n' \
            | sort | paste -sd, -)"
          echo "configs=${configs}" >> "$GITHUB_OUTPUT"
          echo "melange configs: ${configs:-<none>}"

      # Same melange step as the nightly, so a PR that only touches
      # images/debug/tools/ is proven to package and install before merge.
      # x86_64 only here — the PR build is amd64-only too.
      - name: Build local APKs with melange
        if: steps.melange.outputs.configs != ''
        uses: chainguard-dev/actions/melange-build@9d631658f55713e5f63ca0cc21ee168f81301fd9 # v1.6.30
        with:
          multi-config: ${{ steps.melange.outputs.configs }}
          workdir: images/${{ matrix.image }}
          archs: x86_64
          sign-with-temporary-key: true
          signing-key-path: ${{ github.workspace }}/images/${{ matrix.image }}/melange.rsa
          repository-path: ${{ github.workspace }}/images/${{ matrix.image }}/packages

      - name: Validate apko.yaml parses
        working-directory: images/${{ matrix.image }}
        # apko has no dedicated `lint` subcommand; show-config parses + normalizes
        # the YAML and fails on invalid schema/refs, which is functionally a lint.
        run: apko show-config apko.yaml > /dev/null

      - name: Build image tar (amd64) with SBOM
        working-directory: images/${{ matrix.image }}
        run: |
          apko build \
            --arch amd64 \
            --sbom-path . \
            apko.yaml \
            "pr-${{ github.event.pull_request.number || 'manual' }}/${{ matrix.image }}:${{ github.sha }}" \
            image.tar

      - name: Load into Docker (for scanners that want a daemon ref)
        id: load
        working-directory: images/${{ matrix.image }}
        run: |
          out=$(docker load -i image.tar)
          echo "${out}"
          ref=$(echo "${out}" | sed -n 's/^Loaded image: //p' | head -n1)
          echo "ref=${ref}" >> "$GITHUB_OUTPUT"

      # Report-only for now — see AGENTS.md rule 11 for why a Trivy-based gate
      # would be blind to the packages we build ourselves. The counts land in the
      # job summary; flip fail-on to `critical` once the baseline is trusted.
      - name: Scan for CVEs
        uses: ./.github/actions/scan-image
        with:
          image: ${{ steps.load.outputs.ref }}
          tar: images/${{ matrix.image }}/image.tar
          label: ${{ matrix.image }}
          fail-on: none

      - name: Upload scan reports
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: scans-${{ matrix.image }}
          path: |
            trivy.json
            grype.json
          retention-days: 7

      - name: Upload build artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: pr-${{ matrix.image }}
          path: |
            images/${{ matrix.image }}/image.tar
            images/${{ matrix.image }}/*.spdx.json
          retention-days: 7

  # One aggregated comment for every scanned image, edited in place on each run
  # rather than a new comment per commit. Runs after the matrix so the PR gets a
  # single report instead of one comment per image.
  report:
    name: comment CVE report
    needs: build-and-scan
    if: ${{ !cancelled() && github.event_name == 'pull_request' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # Not merge-multiple: every artifact contains trivy.json / grype.json, so
      # merging would overwrite. One directory per image is what scan-report.sh
      # walks.
      - name: Download scan reports
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: scans-*
          path: scans/
        continue-on-error: true # no artifacts when every image was skipped

      - name: Render report
        run: ./.github/scripts/scan-report.sh scans/ > report.md

      # A fork PR gets a read-only token, so posting would fail on something that
      # is only a convenience — the job summary already carries the same table.
      - name: Post sticky comment
        if: github.event.pull_request.head.repo.full_name == github.repository
        uses: ./.github/actions/pr-comment
        with:
          marker: cve-report
          body-file: report.md

      - name: Note when commenting is skipped
        if: github.event.pull_request.head.repo.full_name != github.repository
        run: |
          echo "::notice::Fork PR — CVE report is in the job summary, not a comment."
          cat report.md >> "$GITHUB_STEP_SUMMARY"
