# CLAUDE.md

This repo uses [AGENTS.md](AGENTS.md) as the single source of AI-agent
instructions (repo layout, rules, common tasks, gotchas). Claude Code reads
this file automatically; the `@AGENTS.md` import below pulls the full
content into context so there is no duplication to keep in sync.

@AGENTS.md
